# Sandbox Render and Result Fix

## Fixes
- restored sandbox marker controls in the rendered HTML so the sandbox section appears again
- restored sandbox-side evaluation for check / checkmate / stalemate / repetition based on current board and current turn

## Note
Sandbox still allows free editing, but board evaluation now behaves like analysis on the current position rather than suppressing final-state logic.
