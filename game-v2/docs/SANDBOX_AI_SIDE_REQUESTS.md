# Sandbox AI Side Requests

## Change
Sandbox AI is no longer requested as a single generic move tied awkwardly to turn flow.

## New behavior
Sandbox now exposes:
- Ask AI White
- Ask AI Black

The requested side is passed explicitly into the engine request for sandbox mode.
