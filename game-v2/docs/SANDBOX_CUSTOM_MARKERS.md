# Sandbox Custom Markers

## Purpose
Sandbox pieces can now carry a purely visual custom marker with no gameplay effect.

## Behavior
- select a piece
- type marker text
- apply marker to that piece
- marker follows the piece when it moves
- clear marker when needed
- long labels are shortened for badge display

## Example
- `iron pawn` -> visible badge like `I.P`
