# Specialized One-Panel Setup

## UX decision
There is only one specialized assignment panel.
It lives in the right panel.

## Toggle behavior
The specialized toggle in the left panel (session creation area) controls whether the right-panel assignment setup appears.

## Auth behavior
If the user is not logged in, session creation controls are hidden.
Only the session list/history remains visible.
